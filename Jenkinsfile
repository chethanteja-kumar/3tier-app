pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                echo 'Repository Cloned'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Started'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment Started'
            }
        }
    }
}
